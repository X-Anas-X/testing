<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/styles/style.css" />

  <title>home</title>
</head>

<body>
  <h1>Hello world</h1>

  <nav>
    <a class="search" href="/searches/new"> search for Book</a>
    <a class="home" href="/">Home</a>
  </nav>
  <main>
    <p>Total Books: <%= bookList.length %>
    </p>
    <% booklist.forEach(book=> {%>
      <section class="bookCard">
        <img src="<%= book.image %>" />
        <h2>
          <%=book.title %>
        </h2>
        <h3>
          <%= book.authors %>
        </h3>
        <p>
          <%= book.description %>
        </p>
        <a style="color:red;" href="/books/<%= book.id %>">View Details</a>
      </section>
      <%})%>
  </main>
</body>

</html>